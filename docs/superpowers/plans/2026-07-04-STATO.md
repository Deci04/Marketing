# STATO — Zernio KPI live + redesign box (07-07)

## Committato su branch `serata/multi-feature`
- `1c594fa` serata multi-feature · `dee1e22` Zernio live + audit KPI + disconnect + inventario

## Zernio: VERIFICATO DAL VIVO ✅
- Connect/disconnect social funziona (Luca IG `lucademarco.cf`, 317 follower connesso).
- KPI reali OK dopo audit: engagement 7.5%, non-follower 96.7%, demografiche % (age 18-24=54.6%), demog. sommano 100%.
- Fix applicati: getDefaultProfileId→`/profiles`, breakdown `follow_type`, finestra 89gg, demografiche %, box reach non-follower, tile engagement fallback account.

## Piano box KPI (direttiva utente: prima DIRETTI Zernio → poi DERIVATI → poi MANUALI; box movibili invariati)
Inventario completo: `docs/.../2026-07-07-zernio-data-inventory.md` (~13 fonti, ~90 campi). Scoperta: account-insights ha **12 metriche** (usavamo solo reach). Reali Luca: views 41337, accounts_engaged 753, total_interactions 1732, likes 1284, saves 112, shares 131, reposts 24.
Decisione utente: costruire **TUTTI** i box (inclusi extra), numero-singolo con **valore + delta vs periodo prima**.
- **ONDATA 1 (da fare)**: 12 metriche account-insights → `Measurement` (+ delta: fetch finestra corrente + precedente); demografiche `city` + `engaged` → `AudienceSegment`; esporre in `getKpiData`; poi box UI (numero+delta) + demografiche nuove. NO migration.
- **ONDATA 2 (da fare)**: per-post ranking + extra (best-time, posting-frequency, content-decay, watch-time reel, storie, health) → nuovo modello `ZernioSnapshot` (Json, migration additiva applicata deliberatamente) + box.
⚠️ Subagenti falliti 2× (infra sotto stress, 0 tool / output-injection) → Ondata 1 va fatta inline o in sessione fresca.

## Restano (prod-gated): Google entrata (webhook), bot Telegram (per ultimo, l'utente spiega l'integrazione), W post reale, M mobile, P#3/#4/#5.

---

# STATO serata 2026-07-04 — codice completo + browser-verify parziale

## 🔎 Browser-verify dal vivo (localhost) — esiti
- **H (home)** ✅ verificato. Fix in corsa: bug copy "1 material**i**"→"materiale", "1 montat**i**"→"montato". Nota: "da revisionare" mostrava correttamente 1 contenuto perché "Reel 6" ha `deliveredAt` (dato di test).
- **S (ricerca+archivio)** ✅ verificato dall'utente.
- **N + Integrazioni (profilo)** ✅ compaiono.
- **Google Calendar (G)** ✅✅ VERIFICATO DAL VIVO contro Google reale: OAuth+refresh token, calendario dedicato "Contenuti · Luca" (`6519edef…@group.calendar.google.com`), **uscita** (evento→Google con tag/colore, `syncStatus SYNCED`), **backfill 27 item**. Entrata (Google→tool) resta prod-gated.
- **Zernio** ⚠️ key VALIDA (`hasAnalyticsAccess:true`, base `https://zernio.com/api/v1` corretta) ma `accounts:[]` (nessun social connesso). SCOPERTA: endpoint reali ≠ assunti → `fetchAnalytics`/connect vanno ADATTATI (reali: `/analytics/get-daily-metrics`, `/analytics/instagram/account-insights`, `/analytics?postId=`; connect vuole `profileId`).

## 🛠️ Fix applicati durante il verify (non committati)
- **BUG RUNTIME grosso**: `googleapis` (Node-only) finiva nel bundle CLIENT via `chat-panel`/`diary-chat` → `describeAction` importato da `chat-write-tools`→`content.ts`→`google-calendar`. Fix: estratto `describeAction`+helper in `src/lib/chat-describe.ts` (puro); client importano da lì; + `serverExternalPackages` in `next.config.ts`. **/login era 500, ora 200.** [i test mockavano googleapis → non l'avevano visto]
- **W F1/F2** (dal review): claim atomico anti-republish/doppio-submit in `publishContentAction` (OR null, non `in:[null]`); guardrail anti-proxy dentro il try in `publish()`.
- **G backfill**: nuova `syncAllForWorkspace` + agganciata al callback di connessione.
- Timeout test `zernio-ingest` a 20s.

## 🔧 Aggiornamento sessione 07-06
- **Zernio ADATTATO agli endpoint reali** (da OpenAPI): `fetchAnalytics` aggrega `/accounts/follower-stats`+`/analytics/daily-metrics`+`/analytics/instagram/account-insights`+`/analytics/instagram/demographics`+`/analytics`; ER in %; `getConnectUrl`→`/connect/{platform}` con profileId+redirect_url; callback su param reali. 19 test verdi. TODO da confermare coi dati di Luca: `externalId=latePostId ?? _id`, non_follower_pct single-value, geo da country.
- **Caccia bug (P) fatta** + fix applicati: **#1** `registerWatch` agganciato al callback (entrata Google, attiva in prod); **#2** modale non mostra "Pubblica" ai non-admin (isAdmin/publishState/externalId passati, gate `?? false`) — evita Blob orfano; **#6** `NEXT_PUBLIC_APP_URL` su Vercel+.env. Corretto bug copy mio "montatoi"→"montati".
- **P findings aperti** (per la sessione deploy): **#3** race entrata Google (serve `@@unique([workspaceId,googleEventId])` + upsert); **#4** `resolveWorkspaceForChat` non deterministico (pinna il workspace al link); **#5** codice link Telegram senza scadenza.
- Tree: `tsc` 0 errori, test pure verdi. Tutto ancora **non committato**.

## ⏳ Restano (prod-gated o Luca-gated) → sessione dedicata con DEPLOY
- Google **entrata** (webhook push, URL pubblico) · **Zernio** (adattare endpoint reali + Luca connette social) · **Telegram bot** (setWebhook prod + test) · **W** (post reale, no-ricompressione).

---

# STATO serata 2026-07-04 — codice completo (dettaglio filoni sotto)

## Traguardo: TUTTO il codice autonomo è costruito e verde
Working tree **stabile, non committato** (regola keep-local). `tsc` pulito; suite ~211/212 (unico rosso: flake timeout `zernio-ingest` su Neon condiviso, passa in isolamento — in fix).

## Filoni — stato finale
| Filone | Stato |
|--------|-------|
| **Fondamenta** | ✅ migrata su prod (`20260704021206_fondamenta_serata`) + stub |
| **H** Home per-ruolo | ✅ implementato + review + **fix** (copy singolare, test-confine). Soglia deadline **7 giorni** (deciso) |
| **S** Ricerca+archivio | ✅ implementato + review **PASS** + fix paginazione |
| **T** Diario/Telegram | ✅ Task 1-9 (webhook, diario chat, vision foto, linking lib). Bottone profilo fatto nel coordinamento |
| **Z** Zernio KPI | ✅ Task 1-3 (ingestione) + Task 4 (connect/callback, nel coordinamento). 🔴 pending: `engagement_rate` ×100 (confermare unità coi dati reali) |
| **N** Notifiche | ✅ `notifyTelegramForActivity` acceso, 11/11 test |
| **G** Google Calendar | ✅ implementato + review + **fix** (412 conflitto → last-write-wins; hardening). 37 test |
| **Coord. UI** | ✅ profilo "Integrazioni" (Google/Zernio/Telegram) + connect Zernio + refresh KPI |
| **W** Pubblicazione | ✅ `publish()` qualità-first (proxy mai usato, doppia garanzia). **W review + cleanup in corso** |
| **M** Mobile | ⬜ da fare (per ultimo, dopo browser-verify) |
| **P** Caccia bug | ⬜ da fare (per ultimo) |

## PROSSIMO PASSO GRANDE: browser-verify con l'utente
Serve l'app in esecuzione + azioni di Matteo/Luca. Sblocca la verifica reale di tutte le integrazioni:
1. **Google** — click "Connetti Google Calendar" (consenso OAuth di Matteo) → crea calendario "Contenuti · Luca" → test USCITA (item→Google con tag/colore) + ENTRATA (modifica su Google→tool) + conflitto.
2. **Zernio** — Luca connette i social → "Aggiorna dati" (admin) popola i KPI. ⚠️ confermare nomi param callback Zernio + unità `engagement_rate`.
3. **Telegram** — `setWebhook` su prod + "Collega Telegram" (codice → /start) + test testo/foto(vision)/video.
4. **W** — post di prova reale: verificare che Zernio NON ricomprima (qualità non degradata). Gate non negoziabile.
5. **H/S/N** — verifica UI (home per-ruolo, ricerca/archivio, notifiche in-app) — non serve OAuth, solo app in esecuzione.

## Poi: M (mobile, ottimizzazione responsive completa) → P (caccia bug avversariale).

## Fix minori pendenti
- `engagement_rate` ×100 in `mapAccountMeasurements` (dopo conferma unità Zernio).
- Env `NEXT_PUBLIC_APP_URL=https://marketing-ashy-one.vercel.app` (per i link nelle notifiche N) — su Vercel + .env, senza virgolette.
- Env `GOOGLE_WEBHOOK_TOKEN`/`NEXTAUTH_URL` per il watch-channel Google (in prod, all'attivazione).

## Documenti
Design `specs/2026-07-04-serata-multi-feature-design.md` · Runbook `plans/2026-07-04-orchestrazione-subagenti.md` · Piani `plans/2026-07-04-{fondamenta,h-home,s-ricerca,z-zernio,t-diario,g-gcal}.md`
